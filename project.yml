name: Menere
options:
  bundleIdPrefix: com.mplace
  deploymentTarget:
    iOS: "26.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

packages:
  MenerePackage:
    path: MenerePackage

targets:
  Menere:
    type: application
    platform: iOS
    sources:
      - path: App
    settings:
      base:
        INFOPLIST_FILE: App/Info.plist
        CODE_SIGN_ENTITLEMENTS: App/Menere.entitlements
        PRODUCT_BUNDLE_IDENTIFIER: com.mplace.menere
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        DEVELOPMENT_TEAM: Z2FNFL3X73
    dependencies:
      - package: MenerePackage
        product: AppCore
      - package: MenerePackage
        product: UserDomain
      - package: MenerePackage
        product: AuthenticationDomain
    postBuildScripts:
      - name: "Copy GoogleService-Info.plist"
        script: |
          if [ -f "${PROJECT_DIR}/App/GoogleService-Info.plist" ]; then
            cp "${PROJECT_DIR}/App/GoogleService-Info.plist" "${BUILT_PRODUCTS_DIR}/${PRODUCT_NAME}.app/GoogleService-Info.plist"
          fi
        basedOnDependencyAnalysis: false
